import { CompetenceCollaborateur } from './competence-collaborateur.model';

describe('CompetenceCollaborateur', () => {
  it('should create an instance', () => {
    expect(new CompetenceCollaborateur()).toBeTruthy();
  });
});
